<#-- Contains the project description that will be put in the index page. -->
<p>
	The JMS connector is meant for connecting to JMS middleware like Websphere MQ, 
	Sun J2EE, and others. The JMS connector includes functionality such as; JMS 1.1 
	support, support for queue's and topics, read/write and triggering on 
	destinations (a destination is either a queue or a topic), transaction support, 
	support of different message formats, like clear text, XML, base64 and Binary 
	Transformation Channel (BTC) protocols, and request/reply with optional use of correlation id.
</p>

<p>
	Latest version is ${ant["buildinfo.latest.project.version"]}. 
</p>
	
<p>
	All available versions are here: <a href="versions.html">link</a>
</p>

<p>
	Project page in <a href="https://wiki.cordys.com/display/Con/JMS+Connector">CDN</a>.
</p>

